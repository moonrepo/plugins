format = "2"

[metadata]
name = "v2-test"
type = "command-line"

[install]
download_url = "https://example.com/{download_file}"

[platform.linux]
download_name = "base"

# Declared first, but sorts last alphabetically and by range
[[overrides]]
range = "^1.5"
platform.linux.download_name = "caret"

# Declared last, so should win
[[overrides]]
range = ">=1.0.0"
platform.linux.download_name = "gte"

[source]
versions = ["1.0.0"]
