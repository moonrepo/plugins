[package]
name = "common"
version = "0.0.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
indexmap = { workspace = true }
proto_pdk = { workspace = true, features = ["tracing"] }
serde = { workspace = true }
